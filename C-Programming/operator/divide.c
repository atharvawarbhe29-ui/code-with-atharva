#include <studio.h>
int main (){
  int num1 , num2 , division;

printf('enter freist number: ");
  scanf("%d", &num1);

printf("enter second number (denominator shouldn't be zero): ");
scanf("%d", &num2);

division = num1/num2;

printf("the division of obove twonumber is: %d\n", division);
return 0;

}
